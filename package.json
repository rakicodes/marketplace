{
  "name": "marketplace",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --build --verbose tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
		"packages/*"
	]
}
